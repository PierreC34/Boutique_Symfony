{% extends 'base.html.twig' %}

{% block title %}Skull - Produits{% endblock %}

{% block content %}
<div class="container-fluid">
  <div class="row">

  <div class="div-filtre col-md-2 ">
    <div class="sticky-top">
    <h1 class="">Filtre</h1>
      {{form(form)}}

  </div>
  </div>
    <div class="col-md-10 row">
    <h1>Nos produits - La boutique Pi Air Eau</h1>
{% for product in products %}

      <div class="card w-25 m-5 products-liste ">
        <div class="mt-2 m-auto  h-100 fill">
          <img class="card-img-top  h-100 fill " src="assets/images/uploads/{{product.Image}}" alt="Card image cap">
        </div>
        <div class="card-body m-auto ">
          <h5 class="card-title  ">{{product.Name}}</h5>
            <p class="card-text m-auto">{{product.Subtitle}}</p>
              <div class=" mt-3">
                <a href="#" class="price btn btn-primary btn-price m-auto"><strong>{{product.Price / 100}} €</strong></a>
              </div>
        </div>
      <a class="lien-image" href={{path('product',{slug:product.Slug})}}>Consulter le produit</a>
      </div>
{% endfor %}
  </div>
</div>
</div>

{% endblock %}
